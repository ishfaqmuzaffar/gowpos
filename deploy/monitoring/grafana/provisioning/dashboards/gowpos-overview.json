{
  "annotations": {
    "list": []
  },
  "panels": [
    {
      "type": "stat",
      "title": "API Latency (p95)",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{job='backend'}[5m])) by (le))",
          "format": "time_series"
        }
      ],
      "gridPos": { "x": 0, "y": 0, "w": 12, "h": 8 }
    },
    {
      "type": "graph",
      "title": "Request Rate",
      "targets": [
        {
          "expr": "sum(rate(http_requests_total{job='backend'}[5m]))",
          "format": "time_series"
        }
      ],
      "gridPos": { "x": 12, "y": 0, "w": 12, "h": 8 }
    },
    {
      "type": "graph",
      "title": "Frontend bundle size",
      "targets": [
        {
          "expr": "max(gowpos_frontend_bundle_bytes)",
          "format": "time_series"
        }
      ],
      "gridPos": { "x": 0, "y": 8, "w": 24, "h": 8 }
    }
  ],
  "schemaVersion": 38,
  "title": "GowPOS Overview",
  "version": 1
}
